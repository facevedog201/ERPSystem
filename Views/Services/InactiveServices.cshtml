@model IEnumerable<ERPSystem.Models.Service>
@{
    Layout = "_Layout";
    ViewData["Title"] = "Servicios Inactivos";
}

<h2>Servicios Inactivos</h2>

<div class="mb-3">
    <a class="btn btn-secondary" asp-action="Index">Volver a Servicios Activos</a>
</div>

@await Html.PartialAsync("_ServicesTable", Model)


@* <table class="table table-striped">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Precio</th>
            <th>Tipo</th>
            <th>Activo</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Price.ToString("C")</td>
                <td>@item.Type</td>
                <td>
                    <span class="badge bg-danger">No</span>
                </td>
                <td>
@*                     @* Solo Admin puede reactivar
                    @if (User.IsInRole(UserRole.Admin.ToString()))
                    {
                        <a class="btn btn-sm btn-success" asp-action="Activate" asp-route-id="@item.ServiceId">Reactivar</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
 *@