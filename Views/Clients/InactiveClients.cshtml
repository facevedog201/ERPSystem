@model IEnumerable<ERPSystem.Models.Client>
@{
    ViewData["Title"] = "Clientes Inactivos";
}

<h1>@ViewData["Title"]</h1>

<div class="mb-3">
    <a asp-action="Index" class="btn btn-primary me-2">
        <i class="bi bi-arrow-left me-2"></i> Ver Clientes Activos
    </a>
</div>

<!-- Buscador dinámico -->
<div class="row mb-3">
    <div class="col-md-6">
        <input type="text" id="searchInputInactive" class="form-control"
               placeholder="Buscar por Nombre, RUC, Teléfono o Correo..." />
    </div>
</div>

<!-- Loader -->
<div id="loadingSpinner" class="text-center my-3" style="display:none;">
    <div class="spinner-border text-secondary" role="status">
        <span class="visually-hidden">Cargando...</span>
    </div>
</div>

<!-- Contenedor de resultados -->
<div id="clientsContainerInactive">
    @await Html.PartialAsync("_ClientsTable", Model)
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            let typingTimer;
            const doneTypingInterval = 400; // ms

            $("#searchInputInactive").on("keyup", function () {
                clearTimeout(typingTimer);
                const query = $(this).val();

                typingTimer = setTimeout(function () {
                    $.ajax({
                        url: '/Clients/FilterInactiveClients',
                        type: 'GET',
                        data: { search: query },
                        beforeSend: function () {
                            $("#loadingSpinner").fadeIn(150);
                        },
                        success: function (result) {
                            $("#clientsContainerInactive").hide().html(result).fadeIn(150);
                        },
                        complete: function () {
                            $("#loadingSpinner").fadeOut(150);
                        },
                        error: function () {
                            console.error("Error al filtrar clientes inactivos");
                        }
                    });
                }, doneTypingInterval);
            });
        });
    </script>
}
